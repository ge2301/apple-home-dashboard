fix: align overlay buttons with content edge, scale camera tiles for mobile

- Move sidebar/back/menu buttons inward to align with the page content
  edge (left/right: var(--apple-page-padding)) instead of the screen
  edge, so they sit flush with titles and card grids
- Remove the 1.1x overflow multiplier on camera carousel tiles in
  mobile breakpoints (356-555px, 316-355px) so tiles fit within the
  visible carousel area and rounded corners remain fully visible
- Restore 100cqw for the smallest breakpoint (<=315px) where a single
  tile fills the full content width without overflow
