fix: revert broken button/chip overrides, properly handle camera and page overflow

Reverts the previous width/margin override on .permanent-header that
pushed overlay buttons too far inward, and the overflow-x:hidden on
.wrapper-content that clipped horizontally-scrolling chips and carousels.

Instead:
- Use overflow-x:clip on :host to prevent page-level horizontal scroll
  from edge-to-edge elements (carousels, chips) without creating a new
  scroll container that would break nested overflow-x:auto carousels
- Scale the smallest-viewport camera tile to account for page padding
  (100cqw - 2 * page-padding) so it fits within the visible carousel area
  instead of overflowing with clipped rounded corners
